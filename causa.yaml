version: 1

workspace:
  name: causa

project:
  name: workspace-module-terraform
  description: The Causa workspace module providing functionalities for infrastructure projects coded in Terraform.
  language: typescript
  type: package

causa:
  modules:
    '@causa/workspace-core': '>= 0.24.1'
    '@causa/workspace-typescript': '>= 0.14.0'

javascript:
  dependencies:
    check:
      allowlist: [GHSA-9965-vmph-33xx]
    update:
      packageTargets:
        '@types/node': minor
