version: 1

workspace:
  name: causa

project:
  name: workspace-module-terraform
  description: The Causa workspace module providing functionalities for infrastructure projects coded in Terraform.
  language: typescript
  type: package

causa:
  modules:
    '@causa/workspace-core': '>= 0.25.2'
    '@causa/workspace-typescript': '>= 0.15.1'

javascript:
  dependencies:
    check:
      allowlist: [GHSA-9965-vmph-33xx]
    update:
      packageTargets:
        '@types/node': minor
